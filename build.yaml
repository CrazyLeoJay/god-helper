targets:
  $default:
    builders:
      drift_dev:
        options:
          databases:
            # Required: A name for the database and it's path
            my_database: lib/db/DB.dart
#            my_database: lib/db/

            # Optional: Add more databases
#            another_db: lib/database2.dart

      flutter_gen_runner: # or flutter_gen
        options:
          output: lib/gen/assets/ # Optional (default: lib/gen/)
          line_length: 150 # Optional (default: 80)